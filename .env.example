# OpenRouter API key for the live chatbot, shared by the deck's demo slide and
# the companion site (both call OpenRouter directly from the browser with it).
# Use a CAPPED, DISPOSABLE key (set a low spend limit on openrouter.ai) and
# delete it after the defense. This value is baked into the built JS, so anyone
# who opens the deployed deck or site can read it.
VITE_OPENROUTER_API_KEY=
